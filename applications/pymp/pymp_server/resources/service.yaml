apiVersion: v1
kind: Service
metadata:
  name: pymp-server
  namespace: pymp
  labels:
    app: pymp-server
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  - port: 8000
    protocol: TCP
    targetPort: 8000
  selector:
    app: pymp-server