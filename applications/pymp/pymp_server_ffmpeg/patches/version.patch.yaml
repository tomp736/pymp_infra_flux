# version.patch.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pymp-server-ffmpeg
  namespace: pymp
spec:
  template:
    spec:
      containers:
        - name: pymp-server-ffmpeg
          image: ghcr.io/tomp736/pymp/pymp_server:0.2.9-dev
          imagePullPolicy: Always