apiVersion: v1
kind: Service
metadata:
  name: pymp-server-media-registry
  namespace: pymp
  labels:
    app: pymp-server-media-registry
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: pymp-server-media-registry