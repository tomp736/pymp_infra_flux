# version.patch.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pymp-server-media
  namespace: pymp
spec:
  template:
    spec:
      containers:
        - name: pymp-server
          image: ghcr.io/tomp736/pymp/pymp_server:0.2.18-dev
          imagePullPolicy: Always