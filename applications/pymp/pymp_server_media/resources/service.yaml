apiVersion: v1
kind: Service
metadata:
  name: pymp-server-media
  namespace: pymp
  labels:
    app: pymp-server-media
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: pymp-server-media