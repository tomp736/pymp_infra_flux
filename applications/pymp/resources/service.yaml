apiVersion: v1
kind: Service
metadata:
  name: pymp-server
  namespace: pymp
  labels:
    app: pymp-server
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: pymp-server